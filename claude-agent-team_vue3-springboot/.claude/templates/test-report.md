# 测试报告模板（Test Report）

> 由 qa-agent 在完成测试后输出。

---

## 测试报告

**报告ID**：TEST-{TASK-ID}
**关联任务**：{TASK-ID}
**测试时间**：{YYYY-MM-DD HH:mm} ~ {HH:mm}
**测试环境**：dev / staging
**测试类型**：功能测试 / 回归测试 / 性能测试 / 验收测试

---

## 1. 测试概况

| 指标 | 数值 |
|------|------|
| 总用例数 | {N} |
| ✅ 通过 | {N} |
| ❌ 失败 | {N} |
| ⏭ 跳过 | {N} |
| **通过率** | **{XX%}** |

---

## 2. 测试用例详情

### P0 核心用例

| 编号 | 用例名称 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 状态 |
|------|---------|---------|---------|---------|---------|------|
| TC-001 | {用例名} | {步骤} | {数据} | {预期} | {实际} | ✅ / ❌ |
| TC-002 | {用例名} | {步骤} | {数据} | {预期} | {实际} | ✅ / ❌ |

### P1 主要用例

| 编号 | 用例名称 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 状态 |
|------|---------|---------|---------|---------|---------|------|
| TC-010 | {用例名} | {步骤} | {数据} | {预期} | {实际} | ✅ / ❌ |

### P2 边界/异常用例（重构流程必须执行）

| 编号 | 用例名称 | 操作步骤 | 输入数据 | 预期结果 | 实际结果 | 状态 |
|------|---------|---------|---------|---------|---------|------|
| TC-020 | {用例名} | {步骤} | {数据} | {预期} | {实际} | ✅ / ❌ |

---

## 3. Bug 列表

> 如无 Bug，填写"本次测试未发现 Bug"

### BUG-{序号}

- **Bug ID**：BUG-{YYYYMMDD}-{序号}
- **严重级别**：Critical / High / Medium / Low
- **归属 Agent**：backend-agent / frontend-agent / dba-agent
- **关联用例**：TC-{编号}

**问题描述**：{一句话描述}

**复现步骤**：
1. {步骤1}
2. {步骤2}
3. {步骤3}

**预期结果**：{应该是什么}

**实际结果**：{实际是什么}

**附件**：{请求/响应内容、截图、日志片段}

---

## 4. 性能测试结果（仅性能优化流程）

| 接口 | 测试场景 | 并发数 | 优化前 P99 | 优化后 P99 | 提升幅度 |
|------|---------|--------|-----------|-----------|---------|
| GET /system/user/list | 正常查询 | 50 | 3200ms | 180ms | ↑94.4% |

**测试工具**：JMeter / k6 / wrk
**测试时长**：{N} 分钟
**样本量**：{N} 次请求

---

## 5. 回归范围说明（Bug 修复 / 变更时填写）

| 回归范围 | 说明 | 是否全量 |
|---------|------|---------|
| 用户登录相关用例 | 修复了登录计数 Bug，需回归登录流程 | 否（定向）|
| 订单相关用例 | 无变动 | 跳过 |

---

## 6. 测试结论

```
✅ 全部通过 - 建议进入 reviewer-agent 审查阶段

⚠️ 发现 Bug，需修复后重测
   - 阻塞发布的 Bug：{Bug 列表}
   - 不阻塞发布的 Bug：{Bug 列表}

❌ 核心用例失败，不建议进入下一阶段
   - 失败原因：{说明}
```

---

## 7. Bug 修复验证记录（Verify Fix）

| Bug ID | 描述 | 修复 Agent | 修复时间 | 复测结果 |
|--------|------|-----------|---------|---------|
| BUG-001 | {描述} | backend-agent | {时间} | ✅ 已修复 |
| BUG-002 | {描述} | frontend-agent | {时间} | ❌ 仍存在 |
